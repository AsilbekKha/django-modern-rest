[project]
name = "django-test-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "django~=5.2.0",
    "django-csp>=4.0,<5.0",
    "django-modern-rest[pydantic,jwt,openapi]",
]

[tool.uv.sources]
django-modern-rest = { path = "../", editable = true }
